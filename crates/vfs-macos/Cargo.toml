[package]
name = "vfs-macos"
version = "0.1.0"
edition = "2021"

[dependencies]
sync-engine = { path = "../sync-engine" }
sync-domain = { path = "../sync-domain" }
anyhow = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
# We'll use objc2 for FileProvider interaction
objc2 = "0.5"
objc2-file-provider = "0.3"
