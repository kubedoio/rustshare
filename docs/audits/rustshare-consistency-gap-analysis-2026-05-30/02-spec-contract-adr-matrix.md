# Spec / Contract / ADR Matrix

Status values: implemented, partial, missing, conflicting, unclear.

| Source document | Requirement | Expected behavior | Current implementation location | Current status | Existing tests | Missing tests | Severity | Suggested next action |
|---|---|---|---|---|---|---|---|---|
| `docs/adr/02-contract.md` G-01 | Tenant isolation | No request can access/infer another tenant's protected content. | Auth extractors carry `tenant_id`; metadata queries often include tenant; migrations add tenant/RLS; contract tests exist. | Partial | `backend/tests/contracts/tenant_isolation_contract.rs` | Cross-feature tests for modules, shares, search, public links, uploads, admin. | Critical | Expand tenant isolation contract suite across every handler family. |
| `docs/adr/02-contract.md` G-02 | Permission monotonicity | Responses expose only content principal can access. | `PermissionResolver`, file/folder services, share services; module services mostly use file/folder services. | Partial | `search_authorization_contract`, share/folder tests. | Module list/detail/search/attachments permission tests. | Critical | Add permission tests per route and remove unchecked reads except explicitly proven safe. |
| `docs/adr/02-contract.md` G-03 | Auditability | Every security-sensitive action emits durable audit event. | `admin_actions`, `share_access_log`, `user_security_events`, event store. | Partial | `admin_audit_test.rs`; share access tests. | File upload/download/delete/restore, internal share, module create/update/delete, public link expiry/revocation audit tests. | High | Define audit event taxonomy and test every security-sensitive route. |
| `docs/adr/02-contract.md` G-04 | Idempotent retries | Safe retries do not duplicate durable side effects. | Upload session service; `create_folder_or_get`; some duplicate tests. | Partial | `upload_service_duplicate.rs`, `file_service_duplicate.rs`. | Public upload retry, template create retry, share create retry, sync retry tests. | High | Add idempotency keys or documented duplicate handling where absent. |
| `docs/adr/02-contract.md` G-05 | Revocation correctness | Revoked share/link denies subsequent access promptly. | `ShareService`, `UserShareService`, permission resolver checks revoked shares, public sessions validate share. | Partial | `share_link_contract.rs`, group sharing tests. | Existing share-session token after revocation, folder descendants, group revocation, frontend stale cache tests. | Critical | Centralize sharing path and test revocation against active sessions. |
| `docs/adr/02-contract.md` G-06 | Restore viability | Backup/export artifacts restore tenant data and verification result. | Scripts in `scripts/`, restore contract test, storage verification admin modules. | Partial | `restore_contract.rs`, `storage_verification_contract.rs`. | End-to-end restore drill result surfacing, tenant-scoped restore, module metadata restore. | High | Make restore verification operationally visible and test module objects. |
| `docs/adr/02-contract.md` I-03 | Group principal support | Group shares grant access to members only. | `PermissionResolver` group shares; admin groups; group share handlers. | Partial | `group_sharing_test.rs`, contract tests. | Group membership changes invalidating access/cache; module views via group share. | Critical | Test membership add/remove and cached permission invalidation. |
| `docs/adr/02-contract.md` I-04 | Deprovision safety | Disabled/removed users cannot refresh/use tokens or create shares. | Extractors check `disabled_at`; admin disable routes; sessions/devices. | Partial | `admin_users_test.rs`, admin e2e. | Disabled user device token, WebSocket, share session, invite accept, module create tests. | High | Add deprovision contract tests across auth modes. |
| `docs/adr/02-contract.md` F-01/F-02 | File create/replace/version | Logical file record, active version, listing visibility, audit event; replace keeps identity and version history. | `FileService`, metadata store, object store, versions. | Partial | `file_operations.rs`, `versioning_contract.rs`. | Audit event assertion and object/metadata drift assertions per operation. | High | Harden file contract tests with audit and verification checks. |
| `docs/adr/02-contract.md` F-03/F-04 | Rename/move/delete/restore | Identity/history/shares remain, standard listings hide deleted, restore preserves identity/history. | `FileService`, `FolderService`, trash routes. | Partial | `folder_cascade.rs`, `version_restore.rs`. | Share preservation after move/rename/restore; module folder delete/restore. | High | Add lifecycle contract tests for shared files and module-backed folders. |
| `docs/adr/02-contract.md` F-05 | Deterministic listing | Consecutive listings deterministic with pagination. | Folder/file listing routes and sorting helpers. | Unclear | Some frontend sorting tests. | Backend sort/pagination contract tests. | Medium | Define canonical ordering and test backend and UI. |
| `docs/adr/02-contract.md` F-06 | Path safety | Reject traversal and invalid normalization. | Core validation, template path checks, editor validation, upload sanitization. | Partial | Frontend editor validation tests; directory upload tests. | Backend route-level path traversal tests for templates, attachments, public upload, WebDAV. | Critical | Add backend security tests for every file path input. |
| `docs/adr/02-contract.md` F-07/ST-05 | Metadata/blob consistency and verification | Active file version has readable blob and metadata; broken references detectable. | `scripts/verify-metadata.sh`, storage admin verification, object store. | Partial | `storage_verification_contract.rs`. | Module sidecars, attachments, previews/thumbnails, public upload drift tests. | High | Extend verification to all file-backed module artifacts. |
| `docs/adr/02-contract.md` S-01 | Internal shares | Only target users/groups gain granted capability. | `ShareService` plus deprecated `UserShareService`; permission resolver. | Partial/conflicting | Share/group tests. | Tests proving old and new paths behave identically or old path removed. | Critical | Complete unified share service migration. |
| `docs/adr/02-contract.md` S-02/S-03 | Public read/upload-only links | Anonymous reads/uploads only linked scope; upload-only cannot browse unrelated content. | Public share handlers and share sessions. | Partial | `public_upload_only_contract.rs`, `share_link_contract.rs`. | Browser/frontend tests, folder descendant edge cases, share session expiry/revocation. | Critical | Add active session and traversal tests. |
| `docs/adr/02-contract.md` S-07 | Share governance | Admin can restrict public/external sharing at tenant/workspace level. | `tenant_sharing_config` migration, admin security config routes. | Partial/unclear | Some admin config tests. | Enforcement tests on share creation and UI hiding. | High | Connect config to all sharing routes and frontend affordances. |
| `docs/adr/02-contract.md` S-08 | Secret hygiene | Public-link secrets unguessable and not logged plaintext. | `ShareService::generate_token`, logging mostly structured. | Partial | Unit-level coverage unclear. | Token entropy/length tests and log redaction regression tests. | High | Add secret hygiene tests and review logs. |
| `docs/adr/02-contract.md` C-01/C-06 | Device pairing/revocation | Scoped revocable device token, no reusable long-lived secret display after completion, revoke fails access. | Device auth routes, device tokens, desktop docs. | Partial | `device_pairing_contract.rs`, frontend device tests. | WebSocket/device sync revocation window tests. | High | Add end-to-end device lifecycle contract. |
| `backend/docs/SYNC_SEMANTICS.md` | Sync delta/conflict/retry | Cursor/delta API, deterministic LWW conflict, retry-safe client behavior. | `handlers/sync.rs`, sync engine crates, WebSocket tests. | Partial | `backend/server/tests/websocket_*`, sync engine tests. | Multi-device conflict tests covering module-backed files and shares. | High | Bind sync semantics to contract tests. |
| `docs/adr/02-contract.md` Q-01/Q-02 | Search and auth filtering | Search by name/path and only authorized content. | `handlers/search.rs`, search repos, frontend global search. | Partial | `search_authorization_contract.rs`. | Module artifacts, shared folders, deleted/revoked/expired content tests. | High | Expand search fixtures beyond base files. |
| `docs/adr/02-contract.md` A-01/A-07 | Optional permission-aware AI | Core works without AI; AI never bypasses permissions and cites sources. | Optional `ai_service`, AI routes, AI contract test. | Partial | `ai_permission_contract.rs`. | Disabled-AI route/UI behavior, source attribution, deleted/revoked propagation. | Medium/High | Add AI disabled and stale-index tests before expanding AI UX. |
| `docs/adr/0001-ADR.md` D2/D3 | Metadata/permissions authoritative; tenant hard boundary | Permissions not inferred from object layout; tenant is primary durable boundary. | SQL metadata, object store, tenant fields, services. | Partial | Tenant/share tests. | Object prefix/bucket tenant partitioning tests and docs. | Critical | Document actual storage partitioning and verify object keys. |
| `docs/specs/template-modules-system.md` | Module root paths | Predefined modules under documented roots. | Backend defaults `/Workspace/...`; older docs `/Notes`, `/Kanban`; frontend helpers `/Workspace/...`. | Conflicting | `modulePaths.test.ts`. | Contract update or migration tests for legacy paths. | High | Update source docs to `/Workspace/...` or formalize compatibility. |
| `docs/contracts/template-module-contract.md` | Required module fields | Stable schema for modules/templates. | Backend domain/API uses snake_case; frontend normalizes to camelCase. | Partial/conflicting | `modules.test.ts`, registry tests. | API schema contract tests asserting serialized shape. | High | Freeze API schema and generate frontend types from it or normalize explicitly in docs. |
| `docs/contracts/module-ui-contract.md` | Registry-driven sidebar/dashboard | No hardcoded dashboard/sidebar modules; disabled/unauthorized hidden. | Backend module API, frontend left rail; dashboard still uses static registry and hardcoded quick actions. | Partial/conflicting | Module UI tests. | E2E disabled module hidden across rail/dashboard/routes. | High | Make dashboard consume backend registry only. |
| `docs/contracts/editor-api-contract.md` | Document APIs | Document get/save/attachment APIs if file APIs insufficient. | No `/api/editor/documents` routes found; frontend uses file/note/module APIs and adapter helpers. | Missing/unclear | Frontend editor tests. | Backend editor API or explicit decision that file APIs satisfy contract. | Medium/High | Decide API strategy; then test document revision and attachments. |
| `docs/contracts/attachment-contract.md` | Attachment invariants | Relative `./attachments/`, write permission upload, read permission view, hidden metadata excluded. | Frontend validation/adapter; notes and kanban have custom attachment shapes; file APIs store attachments as files. | Partial | Frontend attachment/security tests; kanban tests. | Backend attachment path/security/public rendering tests. | High | Implement unified attachment service/API or document module-specific behavior. |
| `docs/adr/0020-canonical-markdown-with-editor-cache.md` | Markdown is source of truth | Editor JSON optional; Markdown valid without cache. | Frontend markdown adapter; backend module files are Markdown; no universal backend editor cache policy. | Partial | Markdown adapter tests. | Backend round-trip and cache-ignore tests. | Medium | Add document serialization contract tests. |
| `docs/adr/0021-file-backed-attachments-and-portability.md` | Portable asset references | Attachments are real files with relative refs and public shares only expose allowed assets. | Frontend helpers generate relative refs; backend public share attachment behavior unclear. | Partial | Frontend tests. | Public note/share attachment exposure tests. | High | Add public renderer security tests. |
| `docs/adr/0022-pdf-export-printable-rendering.md` | Printable view first | Browser print/save PDF first, optional server export later. | `PrintableDocumentView.svelte` tests. | Partial | Printable view tests. | E2E print route/export behavior. | Low | Keep later unless editor launch depends on exports. |
| `docs/PRODUCTION_READINESS.md` and contract O-01/O-03 | Readiness, metrics, backup visibility | Health/readiness for dependencies, metrics, backup/verification status. | `/health`, scripts, tracing layer. | Partial/missing | Some smoke scripts. | Readiness endpoint tests, metrics tests, backup status tests. | High | Build operator status surface before launch. |
